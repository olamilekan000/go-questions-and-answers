# Using Loops – Questions

## Question 1 – What is the general form of a `for` loop in Go?
Describe the three components of a `for` loop in Go and what each part does.

## Question 2 – How can you use `for` to count up and down?
Show how to write `for` loops that count upward and downward over a range of integers.

## Question 3 – How does Go treat `++` and `--` compared to other languages?
Explain how the increment and decrement operators work in Go and how they differ from pre/post operators in languages like C or Java.

## Question 4 – How can a `for` loop in Go behave like a `while` loop?
Describe how to write a `for` loop without the initialization and post statements, and give an example scenario where this is useful.

## Question 5 – What is an infinite loop in Go, and how do you escape it?
Show how to create an infinite loop and how to use `break` to terminate it and `continue` to skip to the next iteration.

## Question 6 – How can `for` be used to implement input loops?
Describe a pattern where you repeatedly prompt a user for input until they enter a sentinel value to exit.

## Question 7 – How do you iterate over collections with `range`?
Explain how `for ... range` works with slices or arrays, and what values you get from it on each iteration.

## Question 8 – How do you iterate over the characters of a string?
Describe how to correctly loop over a string that may contain multi‑byte Unicode characters and why you should be careful with byte indices.

## Question 9 – What role do `break` and `continue` play in loops?
Compare `break` and `continue` in Go loops, including how they interact with nested looping scenarios.

## Question 10 – How can you use a loop to generate the Fibonacci sequence?
Outline a loop‑based approach to generating Fibonacci numbers up to some maximum value using variable updates inside the loop.

